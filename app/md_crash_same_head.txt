# This is title  

This is the body of my second entry.  
>Тест маркдауна - нелёгкая задача  


## This is subtitle  

Проблемы var:  
            ♦ Можно объявлять сколько угодно раз. Это не вызовет ошибок, значение переменной будет попросту переопределено:  
<div class = "code-block">
			
```csharp
var car = "audi";  // Определяем var
var car = "bmw";  
```  
</div>


### Too small to see

 $$x + y = 3$$

## This is subtitle  

Проблемы var:  
            ♦ Можно объявлять сколько угодно раз. Это не вызовет ошибок, значение переменной будет попросту переопределено:  
<div class = "code-block">
			
```csharp
var car = "audi";  // Определяем var
var car = "bmw";  
```  
</div>  

## This is subtitle  

Проблемы var:  
            ♦ Можно объявлять сколько угодно раз. Это не вызовет ошибок, значение переменной будет попросту переопределено:  
<div class = "code-block">
			
```csharp
var car = "audi";  // Определяем var
var car = "bmw";  
```  
</div>  

## This is subtitle  

Проблемы var:  
            ♦ Можно объявлять сколько угодно раз. Это не вызовет ошибок, значение переменной будет попросту переопределено:  
<div class = "code-block">
			
```csharp
var car = "audi";  // Определяем var
var car = "bmw";  
```  
</div>  

## This is subtitle  

Проблемы var:  
            ♦ Можно объявлять сколько угодно раз. Это не вызовет ошибок, значение переменной будет попросту переопределено:  
<div class = "code-block">
			
```csharp
var car = "audi";  // Определяем var
var car = "bmw";  
```  
</div>  

## This is subtitle  

Проблемы var:  
            ♦ Можно объявлять сколько угодно раз. Это не вызовет ошибок, значение переменной будет попросту переопределено:  
<div class = "code-block">
			
```csharp
var car = "audi";  // Определяем var
var car = "bmw";  
```  
</div>  

## This is subtitle  

Проблемы var:  
            ♦ Можно объявлять сколько угодно раз. Это не вызовет ошибок, значение переменной будет попросту переопределено:  
<div class = "code-block">
			
```csharp
var car = "audi";  // Определяем var
var car = "bmw";  
```  
</div>